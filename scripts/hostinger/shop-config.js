/**
 * Optional shop origin for marketing pages.
 * Hostinger .htaccess redirects /tienda etc. to the shop host.
 * Keep this in sync with scripts/hostinger/.htaccess if you change the shop URL.
 */
window.PERFUMAS_SHOP_ORIGIN = "https://tienda.perfumas.com.co";
